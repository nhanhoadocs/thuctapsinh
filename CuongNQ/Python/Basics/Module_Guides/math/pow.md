# `pow`
- Cú pháp :
    ```py
    pow(m,n)
    ```
- Công dụng : nhận vào 2 biến số thực `m` , `n` . Kết quả trả về là <code>m<sup>n</sup></code>
- **VD :**
    ```py
    >>> import math
    >>> a = pow(10,2)
    >>> a
    100.0
    ```